# Suppress moderate dev-toolchain advisories (esbuild/vite chain).
# All affected packages are devDependencies — zero production exposure.
# Remove this when @preact/preset-vite officially supports Vite 7/8
# and do a single tracked upgrade at that point.
audit-level=high
